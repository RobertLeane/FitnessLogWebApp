#!/usr/bin/env node

/**
 * Module dependencies.
 */

var app = require('../app');
var debug = require('debug')('book-code:server');

/**
 * Server is now started in app.js with SSL configuration
 */

console.log('Server started. SSL is configured in app.js');
